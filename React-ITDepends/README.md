# React IT Depends

Minimal React + Vite app to visualize IT dependencies and simulate outages.

Run locally:

```bash
npm install
npm run dev
```

Open http://localhost:5173 and click nodes to toggle failures. Impacted business services are listed below the graph.
